function _defineProperty(obj, key, value) { key = _toPropertyKey(key); if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
function _toPropertyKey(arg) { var key = _toPrimitive(arg, "string"); return typeof key === "symbol" ? key : String(key); }
function _toPrimitive(input, hint) { if (typeof input !== "object" || input === null) return input; var prim = input[Symbol.toPrimitive]; if (prim !== undefined) { var res = prim.call(input, hint || "default"); if (typeof res !== "object") return res; throw new TypeError("@@toPrimitive must return a primitive value."); } return (hint === "string" ? String : Number)(input); }
let print = content => console.log(content);
let addText = text => document.write(text);
let choose = object => document.querySelector(object);
let chooseAll = object => document.querySelectorAll(object);
let selectId = object => document.getElementById(object);
let selectTagName = object => document.getElementsByTagName(object);
let msg = content => alert(content);
class Nux {
  constructor() {
    _defineProperty(this, "developer", "Made By ©UraSoft Studio 2022");
    _defineProperty(this, "about", 'Nux.js是一个整活+功能向JS库');
    _defineProperty(this, "version", 'v0.1.3');
    _defineProperty(this, "print", content => console.log(content));
    _defineProperty(this, "addText", text => document.write(text));
    _defineProperty(this, "msg", content => alert(content));
    _defineProperty(this, "closePage", () => {
      window.opener = null;
      window.open('', '_self');
      window.close();
    });
    _defineProperty(this, "warn", content => {
      throw content;
    });
  }
}
_defineProperty(Nux, "developer", 'Made By ©UraSoft Studio 2022');
_defineProperty(Nux, "version", 'v0.1.3');
class $$ {}
_defineProperty($$, "id", id => document.getElementById(id));
_defineProperty($$, "tagName", tagname => document.getElementsByTagName(tagname));
_defineProperty($$, "name", name => document.getElementsByName(name));
_defineProperty($$, "class", content => document.getElementsByClassName(content));
let nux = new Nux();
let str = 'abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPGRSTUVWXYZ1234567890';
let arr = str.split("");
let nux_captcha = null;
for (let i = 0; i < 4; i++) {
  let a = Math.floor(Math.random() * arr.length);
  let b = Math.floor(Math.random() * arr.length);
  let c = Math.floor(Math.random() * arr.length);
  let d = Math.floor(Math.random() * arr.length);
  nux_captcha = arr[a] + arr[b] + arr[c] + arr[d];
}
addText(nux_captcha);
print(nux_captcha);
nux.print(nux.developer);
